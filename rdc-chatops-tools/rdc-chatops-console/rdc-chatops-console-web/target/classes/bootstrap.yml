# ==== system level config, never changes, need be used when spring context start =====
spring:
  cloud:
    config:
      enabled: false
##      uri: http://192.168.8.132:8888
#      profile: dev
#      label: trunk
#      discovery:
#        enabled: true             # 默认false，设为true表示使用注册中心中的configserver配置而不自己配置configserver的uri
#        serviceId: config-server  # 指定config server在服务发现中的serviceId，默认为：configserver
