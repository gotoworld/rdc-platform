# ==== system level config, never changes, need be used when spring context start =====
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/chatopser/hsd-config-repo.git
          username: chatopser
          password: chatopser8
          search-paths: hsd-chatopser
          timeout: 5
      name: