server:
  port: 7001
spring:
  application:
    name: hsd-config-server
  cloud:
     config:
        server:
           git:
              username: coder
              password: 88888888
              uri: http://192.168.254.143/common/config-repository.git
              search-paths: '{application}'
        discovery:
                enabled: true
  profiles:
    include: base,third,job,price,trade,activity,pay
    active: base,third,job,price,trade,activity,pay

## [EUREKA]
eureka:
  instance:
    preferIpAddress: true
    instance-id: ${spring.cloud.client.ipAddress}:${server.port}
#    lease-renewal-interval-in-seconds: 10 # 续约到期时间（默认90秒）
#    lease-expiration-duration-in-seconds: 30 # 续约更新时间间隔（默认30秒）
  client:
    registerWithEureka: true #服务注册
    fetchRegistry: true #服务获取
    serviceUrl:
      defaultZone: http://${security.user.name}:${security.user.password}@localhost:5001/eureka/
#    healthcheck:
#      enabled: true # 开启健康检查（需要spring-boot-starter-actuator依赖

## [SECURITY]
security:
     basic:
       enabled: false
     user:
       name: admin
       password: admin


## [MANAGEMENT]
management:
  security:
    enabled: false

## [LOG]
logging:
  level.root: debug
  path: ./logs/hsd-config-server2/



## [INFO]
info:
  name: "@project.name@"
  description: "@project.description@"
  version: "@project.version@"
  spring-boot-version: "@project.parent.version@"

## [OTHER]
hsd:
  common:
    appPrefix: dev_   #指定app前缀（开发中可使用IP），系统在队列名、Cache名等前会自动加上前缀，以便不同环境冲突